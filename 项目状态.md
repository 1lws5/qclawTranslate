# qclawTranslate 项目状态

> 最后更新:2026-07-09 22:10
> 规则:每次对项目操作后必须更新本文,与 `修改日志.md` 互补--修改日志记录"做了什么",本文记录"现状如何"
> ⚠️ **重要:已执行全面回档至 v1.5.1(91f243a),放弃 2026-07-09 晚间所有修改**

---

## 一、运行状态

| 项目 | 状态 |
|------|------|
| 进程 | ✅ 运行中(v1.5.2) |
| exe 文件 | `C:\Users\Administrator\Desktop\qclawTranslate\qclawTranslate.exe` - **v1.5.2**(2026-07-09 22:10 打包) |
| 源码 | `D:\qclawTranslate\main.py` - **v1.5.2**(基于 v1.5.1 + 3 项修复) |
| 热键 | Ctrl+Q / Ctrl+E(GetAsyncKeyState 50ms 轮询) |
| 翻译 API | DashScope `qwen3.6-flash` + 旧 API key(config.py 中硬编码) |
| TTS | CosyVoice WebSocket 流式 `longanyang` 音色 + MCI 播放 |
| 托盘 | ✓ 系统托盘 + 最小化到托盘 |
| 自启 | `start_on_boot: false` |

## ⚠️ 回档说明

**2026-07-09 21:38 执行全面回档**,原因:
- 用户在 21:38 明确要求"越修越乱,回档"
- 2026-07-09 晚间(20:35-21:38)进行了多轮仓促修改,问题叠加
- 被回滚的修改已 git stash 备份,stash 消息:"备份:API key 修改和 comtypes 打包尝试(2026-07-09 21:38)"

**被回滚的内容**:
1. API Key 替换(新 key → 旧 key)
2. comtypes 打包修复尝试(pip install、预生成、.spec 修改)
3. 4 次重复打包(均失败)
4. 未解决的 UI 热键提示文字不一致问题

**当前状态**:源码纯净回滚至 v1.5.1,exe 已废弃,需重新评估打包方案。

## 二、⚠️ 紧急待办（技术顾问 Phase 0）

| # | 任务 | 顾问要求 | 状态 |
|---|------|---------|------|
| 0.1 | 从 config.py 移除 API Key 硬编码，config.json 加入 .gitignore | 🔴 安全漏洞 | ❌ 未做 |
| 0.2 | 提交改动到 git | 🔴 丢失风险 | ❌ 未做 |
| 0.3 | 版本号已修正为 v1.5.2 | ✅ 已做 |
| 0.4 | 重新打包 exe 并部署 | ✅ 已做 (v1.5.2) |
| 0.5 | Edge + Anki 真机验证 | 🔴 从未验证 | ❌ 未做 |

## 二、🆕 新增紧急待办（回档后）

| # | 任务 | 来源 | 状态 |
|---|------|------|------|
| 0.6 | 解决 PyInstaller 打包 comtypes 动态生成模块问题 | 2026-07-09 打包失败 | ❌ 未做 |
| 0.7 | 评估是否改用纯 ctypes 替代 comtypes 进行 UIA 调用 | 技术顾问建议 | ❌ 未做 |
| 0.8 | 撰写 comtypes 打包问题分析报告 | 用户要求 | 🔄 进行中 |

## 三、文件结构

```
C:\Users\Administrator\Desktop\qclawTranslate\   ← exe 目录(用户双击启动)
├── qclawTranslate.exe     ← ⚠️ 过期(v1.5.1,07-08 17:44,不含 RegisterHotKey)
├── config.json            ← ✓ 已复制(与源码目录一致)
└── qclawTranslate快捷方式.lnk

C:\Users\Administrator\Desktop\文档区\           ← 技术评审文档
├── qclawTranslate_划词缺陷诊断报告_20260708.md
├── qclawTranslate_划词缺陷诊断报告_评审意见.md
├── _copy_selection_全链路诊断报告_20260708.md
├── _copy_selection全链路诊断报告_评审意见.md
├── 内部选中文字存储库_技术方案_20260708.md
├── 内部选中文字存储库_技术方案_评审意见.md
├── 对技术人员回应的二次评审_20260708.md
└── 对技术顾问评审意见的回应_20260708.md

D:\qclawTranslate\          ← 源码 + git 仓库
├── main.py                ← 主程序(1167行,51596字节)- 含 v2.0 改动
├── config.py              ← 配置(含 PyInstaller frozen 路径修复)
├── config.json            ← 用户真实配置
├── qclawTranslate.spec    ← PyInstaller 规格文件
├── qclaw.ico              ← 图标
├── error.log              ← 运行时日志
├── 修改日志.md            ← 详细修改记录
├── 项目状态.md            ← 本文
├── build/                 ← PyInstaller 临时文件
├── diag_*.py              ← 诊断脚本(8个)
├── poc_*.py               ← POC 验证脚本(4个)
├── test_*.py              ← 测试脚本(7个)
└── _diag_runner.py        ← 诊断运行器
```

## 四、源码关键函数索引

| 行号 | 函数 | 作用 |
|------|------|------|
| 343 | `_get_selected_text()` | 托盘菜单划词入口 |
| 350 | `_get_selected_with_position()` | 划词+坐标(工具栏用) |
| 448 | `_find_text_pattern_recursive(uia, element)` | 递归查找 UIA TextPattern |
| 480 | `_uia_extract_selection(tpi)` | 从 TextPattern 偏移截取选中文字 |
| 507 | `_read_selection_uia(hwnd)` | UIA 双路径读选中(GetFocusedElement → ElementFromHandle) |
| 541 | `_read_selection(hwnd)` | 分派器:UIA 优先 → Ctrl+C 回退 |
| 550 | `_bring_to_front_hwnd(hwnd)` | AttachThreadInput 切前台 |
| 570 | `_send_ctrl_c_sendinput()` | SendInput 模拟 Ctrl+C |
| 595 | `_copy_selection_fallback(hwnd)` | Ctrl+C 搬运 + 剪贴板轮询 + 恢复 |
| 616-620 | 热键常量 | `HOTKEY_TRANSLATE_ID=1`, `HOTKEY_TTS_ID=2`, `VK_Q=0x51`, `VK_E=0x45` |
| 776 | `_NativeEventFilter` | QAbstractNativeEventFilter 拦截 WM_HOTKEY(x64 MSG 结构) |
| 945 | `_quit()` | UnregisterHotKey + tray.hide + quit |
| 987 | `_setup_hotkeys()` | RegisterHotKey Alt+Q / Alt+E |
| 999 | `_hotkey_translate_action()` | Alt+Q → 读选中 → 翻译 |
| 1015 | `_hotkey_tts_action()` | Alt+E → 读选中 → 朗读 |

## 五、核心技术栈

| 层 | 源码方案(v2.0) | exe 实际方案(v1.5.1 过期) | 状态 |
|---|---|---|---|
| 热键 | `RegisterHotKey` + `_NativeEventFilter` 拦截 `WM_HOTKEY`,Alt+Q/E | `GetAsyncKeyState` 50ms 轮询,Ctrl+Q/E | ⚠️ 源码已改、exe 未更新 |
| 选中读取路径1 | UIA `GetFocusedElement()` → 穿透 Qt/WebView → TextPattern 偏移截取 | 无此路径(v1.5.1 用 ElementFromHandle) | ⚠️ 源码已改、exe 未更新 |
| 选中读取路径2 | UIA `ElementFromHandle(hwnd)` → TextPattern 偏移截取 | 同 | ✓ |
| 选中回退 | `_copy_selection_fallback`:SendInput Ctrl+C → 剪贴板轮询 1.2s → 恢复 | 同 | ✓ |
| 翻译 | DashScope Compatible API (`qwen3.6-flash`) | 同 | ✓ |
| TTS | CosyVoice WebSocket `ws-c80jgdae5squls8x` / `cosyvoice-v3-flash` / `longanyang` | 同 | ✓ |
| 播放 | MCI 无窗口播放(`winmm.mciSendStringW`) | 同 | ✓ |
| UI | PyQt5 + QSS 暗黑主题(2px 边框、大圆角、hover 动效) | 同 | ✓ |
| 托盘 | QSystemTrayIcon + QMenu(显示窗口/设置/退出) | 同 | ✓ |
| 单实例 | `CreateMutexW` + `GetLastError` | 同 | ✓ |

## 六、config 配置

```json
{
  "translate_api_url": "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions",
  "translate_api_key": "sk-ws-H.RPXHRRM...",
  "translate_model": "qwen3.6-flash",
  "tts_ws_url": "wss://ws-c80jgdae5squls8x.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference",
  "tts_api_key": "sk-ws-H.RPXHRRM...",
  "tts_model": "cosyvoice-v3-flash",
  "tts_voice": "longanyang",
  "start_on_boot": false,
  "tray_minimize": true
}
```

config.py 已修复 PyInstaller frozen 路径:`sys.frozen` 时用 `sys.executable` 定位 exe 目录。

## 七、已知问题(含技术顾问 2026-07-09 全面评析)

### 🔴 阻断性问题

| # | 问题 | 顾问评级 | 状态 |
|---|---|---------|------|
| 1 | **API Key 硬编码在 config.py 源码中**,且已进入 git 历史 | 🔴 安全漏洞 | ❌ 未处理 |
| 2 | **exe 过期** - 源码 v2.0 改动未打包进 exe | 🔴 | ✅ 已重新打包 |
| 3 | **git 未提交 v2.0 改动**(+166/-58 行) | 🔴 丢失风险 | ❌ 未提交 |
| 4 | **版本号不一致** - 代码写 v1.4.0,文档标 v2.0,UI 提示 Ctrl+Q 但热键已改 Alt+Q | 🔴 用户困惑 | ❌ 未修 |
| 5 | **Edge/Anki 真机验证缺失** - Ctrl+C 回退从未在 Edge/Anki 上测试 | 🔴 | 待验证 |

### 🟡 重要问题

| # | 问题 | 顾问评级 | 状态 |
|---|---|---------|------|
| 6 | 无 .gitignore,config.json(含API Key)/ build/ / error.log 可能被跟踪 | 🟡 | ❌ 未做 |
| 7 | SSL 证书验证全局禁用(check_hostname=False, CERT_NONE) | 🟡 安全风险 | ❌ 未修 |
| 8 | 28 个诊断/测试脚本散落在源码目录 | 🟡 | ❌ 未清理 |
| 9 | `_get_selected_with_position()` 仍用 `GetFocusedElement()`(已知坏代码) | 🟡 | 待工具栏时修 |
| 10 | 开机自启脚本 Python 路径硬编码 | 🟡 | ❌ 未修 |
| 11 | 多处 `except Exception: pass` 吞掉异常,诊断困难 | 🟡 | ❌ 未修 |
| 12 | CosyVoice WebSocket 单次模式,无连接复用 | 🟡 | 设计问题 |

### 🔵 改进建议

| # | 问题 | 顾问评级 | 状态 |
|---|---|---------|------|
| 13 | 翻译结果无缓存,重复翻译浪费 API 调用 | 🔵 | 后续优化 |
| 14 | TTS 音频数据未缓存 | 🔵 | 后续优化 |
| 15 | UI 未做 DPI 缩放适配 | 🔵 | 后续优化 |
| 16 | `_find_text_pattern_recursive` 搜索深度/宽度硬编码(max_depth=5, max_children=50) | 🔵 | 后续优化 |
| 17 | 翻译引擎缺少错误重试(429/500/503) | 🔵 | 后续优化 |
| 18 | Qwen-Omni 音频 token 定价不明(页面404) | 🟡 | 待调研 |
| 19 | 语音评测 API 详细定价对比未完成 | 🟡 | 待调研 |

### 💡 方向性思考点

| # | 问题 | 顾问意见 |
|---|------|---------|
| 20 | 是否引入配置热重载 | 💡 后续考虑 |
| 21 | 是否支持多翻译引擎切换 | 💡 后续考虑 |
| 22 | 工具栏弹出功能是否值得继续投入 | 💡 热键稳定前不投入 |
| 23 | 是否迁移到 PySide6 | 💡 当前不迁移,新项目考虑 |
| 24 | API Key 明文存储的安全模型 | 💡 分发时需考虑 |

## 八、Git 状态

```
当前提交: 91f243a v1.5.1: UIA+CtrC双路读选中,修复Edge/Anki
未提交改动:
  - config.py: PyInstaller frozen 路径修复(sys.executable)
  - main.py: RegisterHotKey + _NativeEventFilter 替代 GetAsyncKeyState 轮询
  - main.py: _read_selection_uia 双路径(GetFocusedElement 优先 + ElementFromHandle 回退)
  - main.py: _uia_extract_selection 提取为独立函数
  - main.py: _quit() 中 _hk_timer.stop() 替换为 UnregisterHotKey
  - 项目状态.md: 新建
```

## 九、构建命令

```powershell
# 停止旧进程
Get-Process -Name qclawTranslate -ErrorAction SilentlyContinue | Stop-Process -Force

# 打包
$python = "D:\Qclaw\v0.2.32.610\resources\python\python.exe"
Push-Location D:\qclawTranslate
& $python -m PyInstaller --noconfirm --onefile --windowed --name qclawTranslate `
  --add-data "config.py;." --hidden-import comtypes --hidden-import comtypes.gen `
  --hidden-import "comtypes.gen.UIAutomationClient" `
  --hidden-import win32clipboard --hidden-import win32con `
  --distpath "$env:USERPROFILE\Desktop\qclawTranslate" --workpath D:\qclawTranslate\build main.py
Pop-Location

# 复制 config.json 到 exe 目录
Copy-Item D:\qclawTranslate\config.json $env:USERPROFILE\Desktop\qclawTranslate\config.json -Force

# 启动
Start-Process "$env:USERPROFILE\Desktop\qclawTranslate\qclawTranslate.exe"
```

## 十、版本历史

| 版本 | 日期 | 关键变化 |
|------|------|----------|
| v2.0 | 2026-07-09 | RegisterHotKey OS级 Alt+Q/E + GetFocusedElement 优先路径 + config frozen 路径修复(**源码已改、exe 未打包**) |
| v1.5.1 | 2026-07-08 | UIA+CtrC 双路读选中,修复 Edge/Anki |
| v1.5 | 2026-07-08 | _read_selection_uia 偏移截取替代 _copy_selection(屠版重构) |
| v1.4 | 2026-07-08 | 翻译+TTS+热键(GetAsyncKeyState轮询)+UIA+系统托盘 |
| v1.0-v1.3 | 2026-07-04~05 | 初版→CosyVoice WebSocket→稳定性强化 |

## 十一、技术顾问评析摘要(2026-07-09 20:25)

**评审范围**:全项目源码 + 8份评审文档 + 修改日志 + 项目状态 + git 历史

**总评**:⚠️ 项目核心功能可用但存在多处工程化缺陷,需系统性整改

### 评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完成度 | ⭐⭐⭐ | 核心功能已实现,但 Edge/Anki 未验证 |
| 代码质量 | ⭐⭐⭐ | 可读性好,但错误处理和模块化有改进空间 |
| 安全性 | ⭐⭐ | API Key 泄露 + SSL 禁用,需立即修复 |
| 工程化 | ⭐⭐ | 无测试、无 CI、无 README,git 管理不规范 |
| 可维护性 | ⭐⭐⭐ | 文档维护良好,但单体文件过大 |
| 用户体验 | ⭐⭐⭐⭐ | UI 设计精致,暗黑主题友好 |

### 顾问认可的优点
1. UIA 偏移截取方案是正确的创新
2. 诊断流程扎实(6版+14轮迭代,数据驱动决策)
3. 分派器架构合理(UIA优先 + Ctrl+C回退)
4. 热键从轮询升级到 RegisterHotKey 方向正确
5. MCI 无窗口播放 + CreateMutexW 单实例

### 顾问修改方案(4阶段)
- **Phase 0(紧急)**:API Key 移除 + git 提交 + 版本号修正 + 重新打包 + 真机验证
- **Phase 1(1-2天)**:.gitignore + 清理脚本 + 异常日志化 + 自启路径修复 + README
- **Phase 2(3-5天)**:SSL可配置 + 翻译/TTS缓存 + API重试 + 代码拆分
- **Phase 3(后续)**:工具栏 + 多引擎 + 配置热重载 + 连接池

## 十一、诊断脚本清单

| 脚本 | 用途 |
|------|------|
| diag_anki.py | Anki UIA + Ctrl+C 诊断 |
| diag_config.py | config.json 路径诊断 |
| diag_copysel.py | _copy_selection 全链路诊断(6版迭代) |
| diag_find_culprit.py | 查找热键失效根因 |
| diag_full.py | 完整系统诊断(单实例/热键/SendInput/UIA) |
| diag_full_chain.py | 全链路:热键→_read_selection→UIA→Ctrl+C |
| diag_hotkey_owner.py | 热键占用诊断 |
| diag_system.py | 系统级诊断 |
| diag_uia.py | UIA 组件诊断 |
| poc_diag.py | POC 诊断 |
| poc_final.py | POC 最终验证 |
| poc_selection_hook.py | POC 选区监听 |
| poc_toolbar_diag.py | POC 工具栏诊断 |
| poc_verify.py | POC 验证 |
| test_async_keystate.py | GetAsyncKeyState 测试 |
| test_cosyvoice_ws.py | CosyVoice WebSocket 测试 |
| test_hotkey_minimal.py | 最小热键测试 |
| test_llhook.py | LL Hook 测试 |
| test_qt_hotkey.py | Qt 热键测试 |
| test_qt_hotkey2.py | Qt 热键测试 v2 |
| test_tts.py | TTS 测试 |
| test_uia_quick.py | UIA 快速测试 |
| test_uia_selection.py | UIA 选中测试 |
| _diag_runner.py | 诊断运行器 |
