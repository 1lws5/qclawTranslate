# qclawTranslate 项目状态

> 规则：只描述当前代码的实际状态，不描述计划中的改动。每条"已修复/已实现"必须能在修改日志中找到对应记录。

---

## 一、运行状态

### Git 状态
- **当前提交:** `05d9b87` 修复: _send_ctrl_c_sendinput 先释放Alt键再发Ctrl+C
- **远程仓库:** https://github.com/1lws5/qclawTranslate.git (待推送)
- **config.json:** 已从 git 跟踪移除，本地保留，API Key 不会泄露
- **config.py:** DEFAULT_CONFIG 中 tts_api_key 已清空为 ""

| 项目 | 状态 |
|------|------|
| exe 文件 | `C:\Users\Administrator\Desktop\qclawTranslate\qclawTranslate.exe` (2026-07-10 18:40 打包) |
| 源码 | `D:\qclawTranslate\main.py` (含Alt键释放修复) |
| 热键 | Alt+Q / Alt+E (RegisterHotKey + WM_HOTKEY 拦截) |
| 翻译 API | DashScope `qwen-plus` |
| TTS | CosyVoice WebSocket 流式 `longanyang` 音色 + MCI 播放 |
| 托盘 | ✅ 系统托盘 + 最小化到托盘 |
| 自启 | `start_on_boot: false` |
| config.py frozen 路径 | ✅ 已实现 (`sys.frozen` → `sys.executable` 定位) |

## 二、待验证项

| # | 验证内容 | 状态 |
|---|---------|------|
| 1 | Edge: 选中网页文字→Alt+Q→取词翻译 | ❌ 待验证 |
| 2 | Notepad: 选中文字→Alt+Q→UIA路径正常 | ❌ 待验证 |
| 3 | Anki: 选中文字→Alt+Q→取词 | ❌ 待验证 |
| 4 | 干净环境: 双击exe→config.json正确读取（API key不需重填） | ❌ 待验证 |
| 5 | Alt+E 朗读功能正常 | ❌ 待验证 |

## 三、文件结构

```
C:\Users\Administrator\Desktop\qclawTranslate\   ← exe 目录(用户双击启动)
├── qclawTranslate.exe     ← Alt键释放修复版(2026-07-10 18:40)
├── config.json            ← 用户真实配置
└── qclawTranslate快捷方式.lnk

D:\qclawTranslate\          ← 源码 + git 仓库
├── main.py                ← 主程序(Phase1整改后)
├── config.py              ← 配置(含 PyInstaller frozen 路径修复)
├── config.json            ← 用户真实配置(已从git移除跟踪)
├── qclawTranslate.spec    ← PyInstaller 规格文件
├── qclaw.ico              ← 图标
├── error.log              ← 运行时日志
├── 修改日志.md            ← 详细修改记录
├── 项目状态.md            ← 本文
├── build/                 ← PyInstaller 临时文件
└── .gitignore             ← 已配置(config.json, build/, etc.)
```

## 四、源码关键函数索引

| 行号 | 函数 | 作用 |
|------|------|------|
| 343 | `_get_selected_text()` | 托盘菜单划词入口 |
| 350 | `_get_selected_with_position()` | 划词+坐标(工具栏用) |
| 448 | `_find_text_pattern_recursive(uia, element)` | 递归查找 UIA TextPattern |
| 480 | `_uia_extract_selection(tpi)` | 从 TextPattern 偏移截取选中文字 |
| 507 | `_read_selection_uia(hwnd)` | UIA 双路径读选中(GetFocusedElement → ElementFromHandle) |
| 541 | `_read_selection(hwnd)` | 分派器:UIA 优先 → Ctrl+C 回退 |
| 550 | `_bring_to_front_hwnd(hwnd)` | AttachThreadInput 切前台(已不在 fallback 中调用) |
| 570 | `_send_ctrl_c_sendinput()` | SendInput 模拟 Ctrl+C |
| 595 | `_copy_selection_fallback(hwnd)` | Ctrl+C 搬运 + 剪贴板轮询 + 恢复(不切焦点) |
| 599-620 | 热键常量 + `_NativeEventFilter` | `WM_HOTKEY`/`MOD_ALT`/`VK_Q`/`VK_E` + QAbstractNativeEventFilter 拦截 |
| 918 | `_quit()` | UnregisterHotKey + tray.hide + quit |
| 955 | `_setup_hotkeys()` | RegisterHotKey Alt+Q / Alt+E + installNativeEventFilter |
| 968 | `_hotkey_translate_action()` | Alt+Q → 读选中 → 翻译 |
| 980 | `_hotkey_tts_action()` | Alt+E → 读选中 → 朗读 |

## 五、核心技术栈

| 层 | 实际方案 | 状态 |
|---|---|---|
| 热键 | `RegisterHotKey(MOD_ALT, VK_Q/VK_E)` + `_NativeEventFilter` 拦截 `WM_HOTKEY` | ✅ 已实现 |
| 选中读取路径1 | UIA `GetFocusedElement()` → TextPattern 偏移截取 | ✅ 已实现 |
| 选中读取路径2 | UIA `ElementFromHandle(hwnd)` → TextPattern 偏移截取 | ✅ 已实现 |
| 选中回退 | `_copy_selection_fallback`: SendInput Ctrl+C → 剪贴板轮询 → 恢复(不切焦点) | ✅ 已实现 |
| config 路径 | `sys.frozen` 时用 `sys.executable` 定位，否则用 `__file__` | ✅ 已实现 |
| 翻译 | DashScope Compatible API (`qwen-plus`) | ✅ |
| TTS | CosyVoice WebSocket `cosyvoice-v3-flash` / `longanyang` | ✅ |
| 播放 | MCI 无窗口播放(`winmm.mciSendStringW`) | ✅ |
| UI | PyQt5 + QSS 暗黑主题 | ✅ |
| 托盘 | QSystemTrayIcon + QMenu | ✅ |
| 单实例 | `CreateMutexW` + `GetLastError` | ✅ |

## 六、config 配置

```json
{
  "translate_api_url": "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions",
  "translate_api_key": "sk-...",
  "translate_model": "qwen-plus",
  "tts_ws_url": "wss://ws-c80jgdae5squls8x.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference",
  "tts_api_key": "sk-...",
  "tts_model": "cosyvoice-v3-flash",
  "tts_voice": "longanyang",
  "start_on_boot": false,
  "tray_minimize": true
}
```

config.py frozen 路径处理：`sys.frozen` 为 True 时用 `os.path.dirname(sys.executable)` 定位 config.json，否则用 `os.path.dirname(os.path.abspath(__file__))`。

## 七、已知问题

### 🔴 阻断性问题

| # | 问题 | 状态 |
|---|------|------|
| 1 | Edge/Anki 真机验证缺失 | ❌ 待验证 |

### 🟡 重要问题

| # | 问题 | 状态 |
|---|------|------|
| 2 | SSL 证书验证全局禁用 | ❌ 未修 |
| 3 | 诊断/测试脚本散落在源码目录 | ❌ 未清理 |
| 4 | 多处 `except Exception: pass` 吞掉异常 | ❌ 未修 |
| 5 | 开机自启脚本 Python 路径硬编码 | ❌ 未修 |
| 6 | CosyVoice WebSocket 单次模式,无连接复用 | 设计问题 |

### 🔵 改进建议

| # | 问题 | 状态 |
|---|------|------|
| 7 | 翻译结果无缓存 | 后续优化 |
| 8 | TTS 音频数据未缓存 | 后续优化 |
| 9 | UI 未做 DPI 缩放适配 | 后续优化 |
| 10 | 翻译引擎缺少错误重试(429/500/503) | 后续优化 |

## 八、构建命令

```powershell
# 停止旧进程
Get-Process -Name qclawTranslate -ErrorAction SilentlyContinue | Stop-Process -Force

# 打包
Set-Location D:\qclawTranslate
C:\Users\Administrator\AppData\Local\Python\bin\python.exe -m PyInstaller qclawTranslate.spec --noconfirm

# 复制到桌面
Copy-Item "D:\qclawTranslate\dist\qclawTranslate.exe" "C:\Users\Administrator\Desktop\qclawTranslate\qclawTranslate.exe" -Force
Remove-Item "D:\qclawTranslate\dist\qclawTranslate.exe"

# 启动
Start-Process "C:\Users\Administrator\Desktop\qclawTranslate\qclawTranslate.exe"
```

## 九、版本历史

| 版本 | 日期 | 关键变化 |
|------|------|----------|
| v1.5.2 | 2026-07-10 | Phase1整改: RegisterHotKey Alt+Q/E + config.py frozen路径 + _copy_selection_fallback不切焦点 + GitHub推送 |
| v1.5.1 | 2026-07-08 | UIA+CtrC 双路读选中,修复 Edge/Anki |
| v1.5 | 2026-07-08 | _read_selection_uia 偏移截取替代 _copy_selection |
| v1.4 | 2026-07-08 | 翻译+TTS+热键+UIA+系统托盘 |
| v1.0-v1.3 | 2026-07-04~05 | 初版→CosyVoice WebSocket→稳定性强化 |
